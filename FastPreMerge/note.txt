忽略的type有：none, unknown, other
交替检查的type依次是：stimulate, response

meaningMap
typeMap
checkMap


等待检查的type是：stimulate

循环开始

读取一行
当前数据组
这行的code对应的event是什么type？
如果是忽略的type，则跳过；
如果是等待检查的type，则：
如果是开头，则记录在当前数据组开头，
如果是中间，则记录在当前数据组中间，
如果是结尾，则记录在当前数据组结尾，并指向下一组，
等待检查的type进一；
如果不是等待检查的type，则：
如果是刺激，则标记为缺少反应，
如果是反应，则标记为多余反应，
在日志中记录“出现错误，xx行，具体问题是啥啥”；

循环结束


均值，标准差，频次


最终目录下的文件有：
原始ev2文件
工整的行为数据文件
处理数据时的日志文件
数据情况的汇报文件
反应分类后的表格文件
用于数据融合的表格文件
用于数据融合的dat文件